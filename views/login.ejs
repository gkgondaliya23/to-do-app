<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
</head>
<body>
  <div class="container" style="width: 700px; border: 2px solid red">
    <h1 style="color: #52a3ff; text-align: center">Login</h1>
    <div class="errorMsg">
      <% if (message.length > 0) { %>
        <div style="color: red;"><%= message[0] %></div>
      <% } %>
    </div>
  <form action="/auth/login" method="post">
    <div class="row" style="display: flex; flex-direction: column; align-items: center; margin: 10px;">
      <div class="col">
        <label for="email">Email:</label>
      <input
                type="email"
                style="width: 300px; height: 25px; font-size: 15px"
                id="email"
                name="email"
                placeholder="Enter Email"
                value="<%= typeof email != 'undefined' ? email : '' %>"
              />
      </div>
  <br>
      <div class="col">
        <label for="password">Password:</label>
      <input
                type="password"
                style="width: 300px; height: 25px; font-size: 15px"
                id="password"
                name="password"
                placeholder="Enter Password"
                value="<%= typeof password != 'undefined' ? password : '' %>"
              />
      </div>
      <br>
      <div class="col">
        <button style="
    height: 30px;
    width: 70px;
    align-items: center;
    font-size: 18px;
    color: darkorchid;
  " type="submit">Login</button>
      </div>
    </div>

    
  </form>
  <p style="text-align: center; font-size: 20px;">Register Account? <a href="/auth/register">Register</a></p>
  </div>
</body>
</html>